# GoReleaser configuration for TaskWing
# Documentation: https://goreleaser.com
version: 2

# Pre-release hooks
before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go test ./...

# Build configuration
builds:
  - id: taskwing
    binary: taskwing
    main: ./main.go
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ignore: []
    ldflags:
      - -s -w
      - -X github.com/josephgoksu/TaskWing/cmd.version={{.Version}}
      - -X github.com/josephgoksu/TaskWing/cmd.commit={{.Commit}}
      - -X github.com/josephgoksu/TaskWing/cmd.date={{.Date}}

# Archive configuration
archives:
  - id: taskwing-archive
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides: []
    files:
      - README.md
      - LICENSE
      - .taskwing.example.yaml

# Checksums
checksum:
  name_template: "checksums.txt"

# Changelog generation from conventional commits
changelog:
  use: github
  sort: asc
  abbrev: -1
  groups:
    - title: "🚀 Features"
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: "🐛 Bug Fixes"
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: "📚 Documentation"
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: "🧪 Tests"
      regexp: '^.*?test(\([[:word:]]+\))??!?:.+$'
      order: 3
    - title: "🔧 Maintenance"
      regexp: '^.*?(chore|refactor|style|perf|ci|build)(\([[:word:]]+\))??!?:.+$'
      order: 4
    - title: "Other Changes"
      order: 999
  filters:
    exclude:
      - "^Merge"
      - "^merge"
      - "^WIP"
      - "^wip"

# GitHub release configuration
release:
  github:
    owner: josephgoksu
    name: TaskWing
  draft: false
  prerelease: auto
  mode: replace
  disable: false
  skip_upload: false
  header: |
    ## TaskWing {{.Tag}}

    **Full Changelog**: https://github.com/josephgoksu/TaskWing/compare/{{.PreviousTag}}...{{.Tag}}

  footer: |
    ---

    ### Installation

    **Homebrew (macOS/Linux):**
    ```bash
    brew tap josephgoksu/tap
    brew install taskwing
    ```

    **Manual Installation:**
    1. Download the appropriate binary for your platform from the assets below
    2. Extract the archive
    3. Move the binary to a directory in your PATH
    4. Run `taskwing --help` to get started

    **From Source:**
    ```bash
    git clone https://github.com/josephgoksu/TaskWing
    cd TaskWing
    go build -o taskwing main.go
    ```

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser) 🚀

# Homebrew formula
brews:
  - name: taskwing
    description: "AI-Native Task Management - Generate context-aware dev tasks that match your architecture"
    homepage: "https://taskwing.app"
    license: "MIT"
    repository:
      owner: josephgoksu
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    install: |
      bin.install "taskwing"
    test: |
      system "#{bin}/taskwing", "version"

# Scoop bucket for Windows removed - focusing on Linux/macOS only

# Docker images (disabled - uncomment when ready)
# dockers:
#   - image_templates:
#     - "josephgoksu/taskwing:latest"
#     - "josephgoksu/taskwing:{{ .Tag }}"
#     - "josephgoksu/taskwing:v{{ .Major }}"
#     dockerfile: Dockerfile
#     build_flag_templates:
#     - "--pull"
#     - "--label=org.opencontainers.image.created={{.Date}}"
#     - "--label=org.opencontainers.image.title={{.ProjectName}}"
#     - "--label=org.opencontainers.image.revision={{.FullCommit}}"
#     - "--label=org.opencontainers.image.version={{.Version}}"
#     skip_push: true

# Snapcraft (disabled - uncomment when ready)
# snapcrafts:
#   - name: taskwing
#     summary: AI-assisted CLI task manager for developers
#     description: |
#       TaskWing is a command-line task management tool designed for developers
#       who want to organize their work efficiently while leveraging AI assistance
#       for better productivity.
#     grade: stable
#     confinement: strict
#     publish: false
#     license: MIT
